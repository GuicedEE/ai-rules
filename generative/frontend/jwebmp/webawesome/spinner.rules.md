# WaSpinner — WebAwesome (JWebMP Wrapper)

Wrapper for `wa-spinner` loading indicator. Mirrors ../../webawesome/spinner.rules.md.

Usage
- Add to clusters/stacks; configure size, variant/appearance, and label via fluent setters.

Patterns
- Keep CRTP chaining; provide accessible label for screen readers.
