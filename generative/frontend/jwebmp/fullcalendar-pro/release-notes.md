# Release Notes — FullCalendar Pro Rules

## 2.0.0-SNAPSHOT (Docs & Rules)
- Introduced the new `rules/generative/frontend/jwebmp/fullcalendar-pro` topic index to cover Pro-specific features such as resource/timeline plugins, template slots, and WebSocket-backed resource delivery.
- Added modular `.rules.md` files (overview, options/layout, events/resources, Angular integration, testing) that reference the architecture diagrams, the host `FullCalendarPro` wiring, and the enterprise language/platform rules.
- Created a dedicated topic glossary with LLM interpretation guidance so enforced prompt alignments remain scoped to FullCalendar Pro while still referencing the host `GLOSSARY.md` index.
- Linked the documentation loop (`PACT ↔ GLOSSARY ↔ RULES ↔ GUIDES ↔ IMPLEMENTATION`) to the new topic index and the architecture sources to keep future prompt authors honest with the forward-only policy.
