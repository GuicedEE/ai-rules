# Cerial Master Client Release Notes (Forward-Only)

## Unreleased
- Introduced Cerial Master client topic under `rules/generative/data/activity-master/cerial-client` with modular rules (lifecycle, configuration, telemetry, integration, testing) and a topic glossary aligned to the Java 25 GuicedEE client implementation.
- Documented Vert.x publishing defaults (`server-task-updates`, `sender-%d-tasks`), idle timeout bindings, and Config defaults to match the current library code.
- Clarified distinction between the core Activity Master client library (`../client`) and the Cerial Master client addon (this topic).
